/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "res.cloudinary.com",
            "assets.interviewbit.com",
            "cdn.jsdelivr.net",
            "img.icons8.com"

        ]
    }
};

export default nextConfig;
